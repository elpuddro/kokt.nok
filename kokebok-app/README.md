# Kokebok (Svelte + Tauri)

Selve appen. Se [`../README.md`](../README.md) for full dokumentasjon
(krav, kjøring, bygging, næringsdata, arkitektur).

Hurtigstart:

```bash
npm install
npm run tauri dev      # utvikling
npm run tauri build    # bygg installer
```

# Egne notater — Implementeringsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** La brukeren skrive personlige notater per oppskrift (textarea nederst i detaljvisningen, auto-lagret), med et lite 📝-merke på kort som har notat. Persistert via Tauri Store.

**Architecture:** Tynn `$lib/notater.ts` over Tauri Store (`notater.json`: `Record<id, tekst>`), og UI i `+page.svelte` (state, debounce-lagring, notatfelt i detalj, kort-merke). Speiler favoritt/handleliste-mønsteret. Ingen DB-skriving.

**Tech Stack:** Svelte 5 (runes), Tauri 2 (`@tauri-apps/plugin-store`), TypeScript.

**Spec:** `docs/superpowers/specs/2026-06-17-notater-design.md`

**Merk:** Ingen ren logikk å enhetsteste (Store + UI). Verifikasjon: `npm run build` + manuell e2e (Task 4). Frontend-only, ingen `kokt.db`-skriving. Hver task etterlater prosjektet byggbart.

**Anker-fakta (verifisert mot kode):**
- `onMount` (linje ~337) laster favoritter/handleliste/tema. Notater lastes her.
- Recipe-card `.card-img-wrap` (linje ~540) har `card-badge-time` (546) + `card-fav`-stjerne (548). Notat-merke legges her.
- Detalj-panelet: pris-blokken `{#if prisVist}...{/if}` slutter ~linje 717, så `</div>` (718) lukker innholdskolonnen. Notatfelt legges rett etter 717 (nederst i scrollbart innhold).
- Søkefeltet bruker `searchTimer` + `setTimeout`-debounce — samme mønster for notater.

---

## Filstruktur

| Fil | Ansvar | Endring |
|-----|--------|---------|
| `kokebok-app/src/lib/notater.ts` | Tauri Store-wrapper | Create |
| `kokebok-app/src/routes/+page.svelte` | state, debounce-lagring, notatfelt i detalj, kort-merke, stil | Modify |

**Rekkefølge:** Store-wrapper (T1) → state + last + debounce-handler (T2) → notatfelt i detalj + kort-merke + stil (T3) → manuell e2e (T4).

---

## Task 1: Store-wrapper `notater.ts`

**Files:**
- Create: `kokebok-app/src/lib/notater.ts`

- [ ] **Step 1: Opprett modulen**

Create `kokebok-app/src/lib/notater.ts`:
```ts
// Notater persisteres i en JSON-fil i appens data-katalog via Tauri Store
// (samme mønster som favoritter.ts). kokt.db er read-only.
import { load, type Store } from "@tauri-apps/plugin-store";

const FIL = "notater.json";
const NOKKEL = "notater";

let storePromise: Promise<Store> | null = null;
function hentStore(): Promise<Store> {
  if (!storePromise) storePromise = load(FIL);
  return storePromise;
}

/** Last alle notater (oppskriftId → tekst). Tomt objekt ved feil. */
export async function notaterLast(): Promise<Record<number, string>> {
  try {
    const store = await hentStore();
    return (await store.get<Record<number, string>>(NOKKEL)) ?? {};
  } catch (err) {
    console.error("notaterLast feilet:", err);
    return {};
  }
}

/**
 * Sett (eller fjern) notat for en oppskrift. Returnerer NYTT objekt (Svelte 5
 * $state reagerer ikke på mutasjon). Tom/whitespace tekst fjerner nøkkelen, så
 * et tømt notat ikke etterlater et kort-merke. Best effort på lagring.
 */
export async function notatSett(
  id: number, tekst: string, alle: Record<number, string>,
): Promise<Record<number, string>> {
  const nytt: Record<number, string> = { ...alle };
  if (tekst.trim() === "") delete nytt[id];
  else nytt[id] = tekst;
  try {
    const store = await hentStore();
    await store.set(NOKKEL, nytt);
    await store.save();
  } catch (err) {
    console.error("notatSett lagring feilet:", err);
  }
  return nytt;
}
```

- [ ] **Step 2: Bygg**

Run: `cd "<repo>/kokebok-app" && npm run build`
Expected: `✓ built` uten feil.

- [ ] **Step 3: Commit**

```bash
cd "<repo>" && git add kokebok-app/src/lib/notater.ts
git commit -m "feat(notater): Tauri Store-wrapper"
```

---

## Task 2: State + lasting + debounce-handler i `+page.svelte`

**Files:**
- Modify: `kokebok-app/src/routes/+page.svelte` (import ~linje 10; state ~linje 38; handler nær søk-handler; onMount ~linje 337-340)

- [ ] **Step 1: Importer modulen**

I `<script>`, etter tema-importen (`import { temaLast, ... } from "$lib/tema";`), legg til:
```ts
  import { notaterLast, notatSett } from "$lib/notater";
```

- [ ] **Step 2: Legg til state**

Etter `let aktivtTemaId = $state<TemaId>("varm");` (tema-state fra forrige funksjon), legg til:
```ts
  let notater = $state<Record<number, string>>({});
  let notatTimer: any;
```

- [ ] **Step 3: Last ved oppstart**

I `onMount` (rundt linje 337-340), etter `temaValg = await temaLast();`-blokken (etter `applyTema(...)`-linjen), legg til:
```ts
    notater = await notaterLast();
```

- [ ] **Step 4: Debounce-handler**

I `<script>`, ved de andre funksjonene (f.eks. etter `onSearchInput`), legg til:
```ts
  function onNotatInput(id: number, e: Event) {
    const tekst = (e.target as HTMLTextAreaElement).value;
    notater = { ...notater, [id]: tekst };   // umiddelbar reaktiv oppdatering
    clearTimeout(notatTimer);
    notatTimer = setTimeout(async () => {
      notater = await notatSett(id, tekst, notater);
    }, 400);
  }
```

- [ ] **Step 5: Bygg**

Run: `cd "<repo>/kokebok-app" && npm run build`
Expected: `✓ built` (funksjonen brukes i Task 3; state/last virker alt).

- [ ] **Step 6: Commit**

```bash
cd "<repo>" && git add kokebok-app/src/routes/+page.svelte
git commit -m "feat(notater): state + lasting + debounce-handler"
```

---

## Task 3: Notatfelt i detalj + kort-merke + stil

**Files:**
- Modify: `kokebok-app/src/routes/+page.svelte` (notatfelt etter pris-blokk ~linje 717; kort-merke i card-img-wrap ~linje 546; stil)

- [ ] **Step 1: Notatfelt nederst i detalj**

Finn pris-blokken som slutter med `{/if}` (rundt linje 717), rett FØR `</div>` som lukker innholdskolonnen (linje ~718). Sett inn ETTER pris-`{/if}`:
```svelte
        <section class="detail-notat">
          <div class="detail-notat-title">📝 Mine notater</div>
          <textarea
            class="detail-notat-felt"
            placeholder="Skriv ditt eget notat… (lagres automatisk)"
            value={notater[opp.id] ?? ""}
            oninput={(e) => onNotatInput(opp.id, e)}
          ></textarea>
        </section>
```
(`opp` er `@const opp = currentOppskrift` i detalj-blokken.)

- [ ] **Step 2: Kort-merke**

Finn `.card-img-wrap` (linje ~540). Den inneholder `{#if r.tid}<div class="card-badge-time">⏱ {r.tid}</div>{/if}` (linje ~546) og `card-fav`-knappen. Rett ETTER `card-badge-time`-linjen, legg til:
```svelte
              {#if notater[r.id]}<div class="card-badge-notat" title="Du har et notat">📝</div>{/if}
```

- [ ] **Step 3: Stil**

I `<style>`, etter `.card-fav.aktiv`-regelen (rundt linje 835), legg til:
```css
  .card-badge-notat {
    position: absolute; top: 8px; right: 8px;
    background: rgba(255,255,255,0.92); border-radius: 20px;
    padding: 2px 6px; font-size: 0.8rem; border: 1px solid var(--border-light);
  }
  .detail-notat { margin-top: 24px; }
  .detail-notat-title { font-weight: 700; margin-bottom: 8px; color: var(--text); }
  .detail-notat-felt {
    width: 100%; min-height: 90px; resize: vertical;
    padding: 10px 12px; border: 1px solid var(--border); border-radius: var(--radius);
    background: var(--surface); color: var(--text); font-family: var(--font-ui); font-size: 0.95rem;
  }
  .detail-notat-felt:focus { outline: none; border-color: var(--border-focus); }
```
NB: `card-badge-time` ligger nederst-høyre, `card-fav` øverst-venstre. Notat-merket
øverst-høyre kolliderer ikke. `.card-img-wrap` har `position: relative` fra før.

- [ ] **Step 4: Bygg**

Run: `cd "<repo>/kokebok-app" && npm run build`
Expected: `✓ built` uten feil.

- [ ] **Step 5: Commit**

```bash
cd "<repo>" && git add kokebok-app/src/routes/+page.svelte
git commit -m "feat(notater): notatfelt i detalj + kort-merke"
```

---

## Task 4: Manuell ende-til-ende-verifikasjon

Krever kjørende app. MANUELT (menneske).

**Files:** ingen.

- [ ] **Step 1: Kjør appen**

Run: `cd "<repo>/kokebok-app" && npm run tauri dev`

- [ ] **Step 2: Verifiser (sjekkliste)**

- Åpne en oppskrift → «📝 Mine notater»-felt nederst → skriv noe.
- Lukk oppskriften og åpne den igjen → notatet er der.
- Tilbake i rutenettet: kortet har et 📝-merke.
- Slett all notattekst → merket forsvinner (etter ~0,4 s, tom-nøkkel ryddet).
- Skriv notat på oppskrift A, åpne oppskrift B → B har eget/tomt felt (ikke A sitt).
- Lukk appen helt og start på nytt → notatet består (fra notater.json).

---

## Self-review-notater (utført ved planskriving)

- **Spec-dekning:** Store-wrapper m/`Record<id,tekst>` + tom-rydding (T1), state +
  last + debounce-handler (T2), notatfelt i detalj + kort-merke + stil (T3),
  manuell e2e (T4). Auto-lagre debounced, kort-merke, ingen filter — alt dekket.
- **Navn/typer konsistente:** `notaterLast()`/`notatSett(id,tekst,alle)` definert
  T1, brukt T2/T3. `notater`/`notatTimer`/`onNotatInput(id,e)` konsistent T2→T3.
  `notater[opp.id]` (detalj) og `notater[r.id]` (kort) bruker samme state.
- **Verifisert mot kode:** onMount-anker (~337), card-img-wrap + card-badge-time
  (~540-546), pris-blokk-slutt (~717) som innsettingspunkt for notatfelt,
  `searchTimer`-debounce-mønster gjenbrukt. `.card-img-wrap` har `position:
  relative`. CSS-variabler (`--surface`,`--border-focus`,`--font-ui`) finnes.
- **Ingen ren logikk:** alt er Store + UI → bygg + manuell e2e (som favoritter/
  handleliste). Frontend-only, ingen DB-skriving.